1. 먼저 — 당신의 예시가 알려주는 가장 중요한 사실
GPT-4o 캡션과 실제 사용자 쿼리 비교를 처음 받았을 때부터 마음에 걸렸던 부분인데, 이제 본격적으로 짚어야 할 시점입니다. 두 텍스트의 격차가 정말 큽니다.
GPT-4o 캡션 예시(이벳필드 후드): "이 제품은 이벳필드의 베이직 로고 후드로, 색상은 부드러운 그레이로 이루어져 있습니다. 후드 디자인으로 되어 있어 착용 시 편안함과 따뜻함을 제공합니다. 가슴 부분에는 'Ebbets Field'라는 브랜드 로고가 네이비색으로 프린팅되어 있으며..." — 약 400자, 격식체, 정보 풍부.
실제 사용자 쿼리: "그레이 후드에 진한 네이비 색으로 영어가 가슴쪽에 적혀있고, 그 아래 빨간색 글씨도 적혀있다." — 약 50자, 구어체, 시각적 인상 위주.
이 격차에서 보이는 패턴이 몇 가지 있습니다. 사용자는 브랜드명을 거의 말하지 않습니다(이벳필드라는 이름이 사용자 쿼리에 등장하지 않죠). 소재나 핏 같은 추상적 속성도 잘 안 씁니다(면, 오버핏 같은 단어가 사용자 쿼리에서 거의 안 보임). 대신 사용자는 시각적으로 즉시 보이는 것들을 말합니다. 색깔, 글자가 어디 있는지, 그림이 뭔지, 어떤 모양인지요.
또 하나 결정적인 패턴. 하와이안 셔츠 예시에서 사용자는 "그레이색의 반팔셔츠"라고 말하는데 GPT-4o는 "베이지 색상"이라고 합니다. 즉 사용자의 색 인식이 정답과 어긋나기도 합니다. 이건 학습/평가 양쪽에서 매우 중요한 함의가 있는데, "정답"을 GPT-4o 캡션 기준으로 잡으면 사용자 인식과 안 맞을 수 있다는 뜻이에요.
이 관찰이 당신 로드맵의 핵심을 결정합니다. **본 실험의 가설은 "GPT-4o 캡션 ↔ 사용자 쿼리의 분포 격차가 zero-shot baseline의 주된 약점이고, 이 격차를 좁히는 것이 성능 향상의 가장 큰 레버"**가 됩니다. 매우 명확한 학술적 메시지죠.
2. 전체 시스템 아키텍처 — 한 장에 그리기
당신이 그린 그림(training: query augmentation으로 학습, inference: 사용자 쿼리를 LLM으로 정제 후 검색)을 그대로 받아서 구체화하면 이렇게 됩니다.
Training 시점: 무신사 이미지 3,100장과 GPT-4o 캡션 3,100개가 있습니다. 각 (이미지, GPT-4o 캡션) 쌍에 대해 LLM으로 사용자 스타일의 짧은 쿼리 5~10개를 자동 생성합니다. 그러면 학습 페어가 (이미지, 사용자 스타일 쿼리) 형태로 15,000~30,000개가 됩니다. 이걸 SigLIP 2에 LoRA로 contrastive 학습시킵니다.
Inference 시점: 사용자가 자연어 쿼리를 입력하면("그레이 후드에 진한 네이비 색으로 영어가 가슴쪽에..."), 가벼운 한국어 LLM이 이걸 정규화된 형태로 다듬습니다. 그 다음 학습된 SigLIP 2가 다듬어진 쿼리를 임베딩해서 사전 인덱싱된 무신사 이미지 임베딩들과 cosine similarity로 top-10을 반환합니다.
핵심 통찰: training 시점의 쿼리 augmentation과 inference 시점의 쿼리 정규화가 같은 분포 정렬(distribution alignment) 문제의 양면입니다. 학습할 때는 "사용자 스타일 쿼리 분포"를 모델에 학습시키고, 추론할 때는 "사용자 쿼리를 학습 분포에 맞게 정제"하는 거죠. 이 일관성이 보고서의 일관된 메시지가 됩니다.
3. 모델 선택 — 안전한 길로 가자
본 실험을 안전하게 가져가는 게 목표이니, SigLIP 2 base patch16-256을 추천드립니다. 이유는 세 가지입니다.
첫째, A6000 48GB에서 매우 여유롭습니다. base 모델은 대략 380M 파라미터로, LoRA 학습 시 batch 256까지 충분히 올라가요. So400m도 좋지만 base가 더 빠르고 디버깅도 쉽습니다.
둘째, 64 토큰 제한이 오히려 당신 시나리오와 맞습니다. 사용자 쿼리는 보통 짧으니까 잘릴 일이 거의 없어요. 학습 시에도 augmented 짧은 쿼리들을 쓸 거라서 토큰 제한이 병목이 안 됩니다.
셋째, 당신의 baseline을 "긴 GPT-4o 캡션을 그대로 잘라서 zero-shot SigLIP 2에 넣는 것"으로 잡으면, 본 실험 모델은 같은 SigLIP 2 backbone에 augmented 짧은 쿼리로 학습한 버전이 됩니다. 모델 architecture는 같고 학습 데이터의 분포만 바뀌는 깨끗한 비교가 됩니다. 학술적으로도 매우 깔끔해요.
쿼리 정제용 LLM은 Qwen2.5-7B-Instruct 또는 EXAONE-3.5-7.8B-Instruct 중 한국어 instruction following이 더 강한 쪽을 고르세요. EXAONE이 한국어에 특화되어 있어 무신사 패션 어휘를 더 잘 다룰 가능성이 높지만, Qwen이 더 보편적이고 안정적입니다. 1일차에 두 모델 다 작은 샘플로 한 번씩 돌려보고 결정하시는 게 안전합니다.
4. 데이터 준비 — Augmentation 디테일
이게 본 실험의 성패를 가르는 가장 중요한 부분입니다. LLM에게 그냥 "사용자 쿼리 만들어줘"라고 하면 결과가 들쑥날쑥해서 학습 신호가 noisy해집니다. 당신의 예시 비교 파일이 이 부분에서 진짜 가치가 있는데, few-shot 예시로 넣을 수 있다는 점이에요.
프롬프트 설계는 이런 구조로 가시면 됩니다.
다음은 무신사 상품의 GPT-4o 상세 설명과, 실제 사용자가 그 상품을 검색할 때 입력할 법한 짧은 자연어 쿼리의 예시들이다.

[예시 1]
상세 설명: "이벳필드의 베이직 로고 후드... [전체 캡션]"
사용자 쿼리: "그레이 후드에 진한 네이비 색으로 영어가 가슴쪽에 적혀있고, 그 아래 빨간색 글씨도 적혀있다. 주머니랑 모자 부분의 스트링도 있다."

[예시 2]
상세 설명: "더셔츠하우스의 하와이안 셔츠... [전체 캡션]"
사용자 쿼리: "그레이색의 반팔셔츠인데 작은 호랑이 그림이 덕지덕지 붙여져있다."

[예시 3]
상세 설명: "지블랙의 100 오가닉 코튼 제니스 로고... [전체 캡션]"
사용자 쿼리: "zenith라는 흰색 글씨가 독특한 폰트로 적힌 검은색 반팔 티셔츠"

위 예시처럼, 다음 상세 설명에 대해 사용자가 입력할 법한 짧고 시각적인 한국어 쿼리를 5개 생성하라. 다양성을 위해 각 쿼리는 서로 다른 스타일(매우 짧은 키워드형, 한 문장형, 두 문장형)과 다른 시각적 측면(색상 위주, 그래픽 위주, 전체 인상 위주 등)을 강조하라.

상세 설명: [GPT-4o 캡션]

사용자 쿼리 5개:
이 프롬프트의 핵심은 당신이 가진 7개의 실제 사용자 쿼리 예시를 LLM에게 보여주는 것입니다. 이게 단순히 데이터 증강을 넘어 실제 사용자 분포를 LLM에게 학습 신호로 전달하는 효과를 냅니다. 7개가 적어 보일 수 있지만 in-context learning에서는 충분히 강력합니다.
생성한 쿼리는 반드시 검수하세요. 3,100 × 5 = 15,500개 중 100~200개 무작위 샘플을 직접 읽고, 명백한 hallucination이나 부자연스러운 표현이 5% 미만이어야 학습에 써도 안전합니다. 너무 많으면 프롬프트를 수정해서 다시 생성합니다.
추가 팁: 하와이안 셔츠 예시처럼 사용자가 색을 잘못 인식하는 경우도 augmentation에 일부 반영하면 robust한 모델이 됩니다. LLM에게 "전체 5개 중 1개는 약간 색 인식이 다르거나 핵심 색만 짚을 수 있다"는 지시를 넣어보세요. 이건 옵션이긴 한데, 시간 여유 있으면 시도해볼 가치가 있어요.
5. 학습 디테일 — LoRA 설정과 Loss
SigLIP 2 base에 LoRA를 붙이는 표준 설정으로 갑니다.
LoRA 타겟: vision tower와 text tower 양쪽의 attention projection (q_proj, k_proj, v_proj, out_proj)에 r=16, alpha=32, dropout=0.05.
학습 가능 파라미터: 전체의 약 0.5~1% 수준. 3,100개 이미지로도 충분히 학습 가능한 규모입니다.
Loss: SigLIP 2의 표준 sigmoid loss를 그대로 사용. 추가 손실 없이 단순하게.
Batch size: A6000에서 BF16 기준 256~384까지 가능. 256으로 시작하세요.
Learning rate: LoRA 학습은 일반 fine-tuning보다 lr이 높아도 안전합니다. 1e-4에서 시작해서 cosine decay.
Epochs: augmented 데이터 15,500개에 batch 256이면 epoch당 60 step. 30 epoch 정도가 적당합니다. 총 1,800 step. A6000에서 1~2시간이면 끝납니다.
Train/val 분할: 무신사 카탈로그 3,100개를 product 단위로 80/20 분할(상품 단위가 중요합니다 — 같은 상품의 augmented 쿼리들이 train과 val에 동시에 들어가면 안 됩니다). Train 2,480개 product × 5 쿼리 = 12,400 쌍, Val 620개 product × 5 쿼리 = 3,100 쌍.
학습 중 검증은 val set의 (쿼리, 이미지) 쌍에 대한 R@10을 매 epoch마다 측정하고, 가장 높은 epoch의 LoRA weight를 저장합니다.
6. Inference 시점의 Query Refiner — 가장 절약하기 좋은 부분
여기가 1주일 안에 끝내려면 욕심을 내려놓아야 하는 부분입니다. Query refiner를 별도로 학습시키지 마시고, 순수 prompting으로만 처리하세요. EXAONE이나 Qwen에 다음 같은 프롬프트를 던집니다.
다음 사용자의 한국어 패션 검색 쿼리를 받아, 검색 시스템이 잘 이해할 수 있는 구조화된 형태로 정제하라.

[규칙]
- 사용자가 말한 시각적 정보(색상, 그래픽, 모양)는 모두 보존한다
- 명확한 카테고리 단어를 추가한다 (반팔, 후드, 셔츠 등)
- 사용자가 사용한 표현을 가능하면 유지하되, 검색에 도움 안 되는 부분은 제거한다
- 한 문장 또는 두 문장으로 정제한다

[예시]
사용자 쿼리: "그레이 후드에 진한 네이비 색으로 영어가 가슴쪽에 적혀있고, 그 아래 빨간색 글씨도 적혀있다."
정제된 쿼리: "그레이 후드, 가슴에 네이비 영어 로고와 빨간색 글씨"

[입력]
사용자 쿼리: {user_query}
정제된 쿼리:
이 정도로도 충분합니다. 별도 학습 없이 인퍼런스 시점에만 작동하니 코드 양이 거의 없어요. ablation에서 "refiner 있음 vs 없음"만 비교하면 됩니다.
만약 시간이 정말 남으면, training augmentation을 만들 때 역방향 페어도 함께 모아두세요. 즉 "정제된 형태의 쿼리"와 "원본 사용자 스타일 쿼리"의 쌍을 1,000개 정도 만들어두면, 나중에 small T5 같은 걸로 refiner를 fine-tuning할 수 있어요. 1주일 안엔 안 할 거지만 future work로 자연스럽게 이어집니다.
7. 평가 셋 — 가장 신경 써야 할 부분
여기서 보고서의 정직성이 결정됩니다. 평가 셋을 augmented 쿼리로 만들면 train/test 분포가 동일해서 점수가 인플레이션됩니다. 반드시 사람이 손으로 쓴 쿼리가 메인 평가가 되어야 해요.
평가 셋 구성:

50개 쿼리를 직접 작성. test split의 620개 product 중에서 무작위로 50개를 골라 직접 봅니다.
각 product에 대해 사용자 스타일로 한국어 쿼리 1개 작성. 당신이 이미 가진 7개 예시의 톤을 유지하세요.
각 쿼리에 대해 정답 이미지를 라벨링. 갤러리 안에 비슷한 상품이 더 있으면 multi-positive로 표시.

평가 갤러리: test split 620개 product (이미지 1장씩) — 정답이 갤러리 안에 들어 있어야 의미 있습니다.
메인 메트릭: Recall@10 단일 지표. 보조로 R@5, MRR도 보고하면 좋지만 메인은 R@10.
평가 조건:

Baseline 1: SigLIP 2 zero-shot (캡션을 64토큰 trim해서 갤러리 인덱싱, 사용자 쿼리는 그대로)
Baseline 2: SigLIP 2 zero-shot + GPT-4o 캡션 갤러리 (같지만 사용자 쿼리도 64토큰 안에 들어옴)
Method 1: SigLIP 2 + LoRA 학습 (augmented 쿼리로 학습)
Method 2: SigLIP 2 + LoRA 학습 + LLM query refiner (full pipeline)

이 네 조건의 R@10이 baseline 1 < baseline 2 < method 1 < method 2 순으로 단조 증가하면 보고서의 메시지가 가장 깔끔합니다.
8. 1주일 일별 로드맵
이제 일정에 맞춰 정리하겠습니다.
1일차 (월): 환경 세팅 + Augmentation 프롬프트 검증. SigLIP 2 base와 EXAONE/Qwen 7B 두 LLM을 A6000에 띄워보고, augmentation 프롬프트로 100개만 시범 생성해서 품질 확인. 프롬프트를 2~3번 수정. 평가 쿼리 50개 직접 작성 시작.
2일차 (화): Augmentation 풀스케일 실행. train split 2,480개 product × 5 쿼리 = 12,400개 생성 (A6000에서 4~6시간). 동시에 평가 쿼리 50개 완성하고 정답 라벨링. 직접 검수 200개.
3일차 (수): Baseline 측정. SigLIP 2 zero-shot 두 조건(baseline 1, 2)을 평가 셋에서 측정. 코드는 transformers 라이브러리로 80줄 정도면 끝납니다. 결과 기록.
4일차 (목): LoRA 학습 첫 실험. peft + transformers로 학습 코드 작성, 30 epoch 돌리기 (1~2시간). val set에서 best epoch 선택. 평가 셋에서 method 1 측정.
5일차 (금): Query refiner 추가 + method 2 측정. 학습 결과가 baseline보다 명확히 좋지 않으면 hyperparameter 조정 (lr, epoch, LoRA r) 1~2회 재학습. ablation 표 정리.
6일차 (토): 정성 분석. 잘 맞춘 케이스 10개와 못 맞춘 케이스 10개 골라서 시각화. 가능하면 Streamlit 데모 30분 만들기. 보고서 figure와 table 초안.
7일차 (일): 보고서 작성. introduction, method, experiments, results, conclusion 구조로. 위 결과들을 채워 넣기. 첫 draft 완성.
9. 위험 신호와 대응
이 계획에서 가장 위험한 두 지점을 미리 짚어드리겠습니다.
위험 1: LoRA 학습 결과가 baseline보다 안 좋게 나오는 경우. 가능성이 30~40%는 있습니다. 이 경우 즉시 의심할 것은 (a) augmented 쿼리의 품질, (b) learning rate가 너무 높음, (c) epoch 부족. 5일차에 hyperparameter 1~2회 조정 시간을 미리 비워둔 건 이 때문입니다.
위험 2: 평가 셋이 너무 쉬워서 모든 모델이 R@10 0.9+ 찍는 경우, 또는 너무 어려워서 모두 0.2 이하인 경우. 50개 쿼리는 이런 극단을 피하려면 난이도 분산이 중요합니다. 짧고 모호한 쿼리("흰 셔츠")부터 길고 구체적인 쿼리("zenith 흰 글씨 검정 반팔")까지 골고루 섞어서 작성하세요.